apiVersion: apps/v1
kind: Deployment
metadata:
  name: vite-app
  namespace: default
s·pec:
  replicas: 2
  selector:
    matchLabels:
      app: vite-app
  template:
    metadata:
      labels:
        app: vite-app
    spec:
      containers:
        - name: vite-app
          image: your-docker-repo/vite-app:latest
          ports:
            - containerPort: 80
